import React, { } from 'react';
import { Editable } from 'interfaces';


interface ButtonProps extends Editable{
  
}

export const Button: React.FC<ButtonProps> = () => {

  return (
    <div>I am Button</div>
  )
} 